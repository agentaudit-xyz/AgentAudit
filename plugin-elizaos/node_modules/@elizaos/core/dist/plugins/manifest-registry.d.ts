/**
 * Plugin Manifest Registry
 *
 * Loads and caches plugin manifests (elizaos.plugin.json).
 *
 * @module plugins/manifest-registry
 */
import type { PluginCandidate, PluginDiagnostic, PluginKind, PluginManifestLoadResult, PluginManifestRecord, PluginManifestRegistry } from "../types/plugin-manifest.ts";
import type { JsonValue } from "../types/primitives.ts";
/** Plugin manifest filename */
export declare const PLUGIN_MANIFEST_FILENAME = "elizaos.plugin.json";
/** Alternative manifest filenames for compatibility */
export declare const PLUGIN_MANIFEST_FILENAMES: readonly ["elizaos.plugin.json"];
/**
 * Configuration for plugins loading.
 */
export interface PluginsConfig {
    /** Plugin allow list */
    allow?: string[];
    /** Plugin deny list */
    deny?: string[];
    /** Additional plugin load paths */
    loadPaths?: string[];
    /** Plugin entries configuration */
    entries?: Record<string, {
        enabled?: boolean;
        config?: Record<string, JsonValue>;
    }>;
    /** Plugin slots configuration */
    slots?: {
        memory?: string;
    };
}
/**
 * Normalized plugins configuration.
 */
export interface NormalizedPluginsConfig {
    allow: string[];
    deny: string[];
    loadPaths: string[];
    entries: Record<string, {
        enabled?: boolean;
        config?: Record<string, JsonValue>;
    }>;
    slots: {
        memory?: string;
    };
}
/**
 * Normalize plugins configuration with defaults.
 */
export declare function normalizePluginsConfig(config: Partial<PluginsConfig> | undefined): NormalizedPluginsConfig;
/**
 * Resolve the path to a plugin's manifest file.
 */
export declare function resolvePluginManifestPath(rootDir: string): string;
/**
 * Load a plugin manifest from disk.
 *
 * @param rootDir - The plugin root directory
 * @returns Load result with manifest or error
 */
export declare function loadPluginManifest(rootDir: string): PluginManifestLoadResult;
/**
 * Load the plugin manifest registry.
 *
 * Discovers all plugin candidates and loads their manifests.
 *
 * @param params - Registry loading parameters
 * @returns Plugin manifest registry
 */
export declare function loadPluginManifestRegistry(params: {
    config?: {
        plugins?: Partial<PluginsConfig>;
    };
    workspaceDir?: string;
    cache?: boolean;
    env?: NodeJS.ProcessEnv;
    candidates?: PluginCandidate[];
    diagnostics?: PluginDiagnostic[];
}): PluginManifestRegistry;
/**
 * Clear the manifest registry cache.
 */
export declare function clearManifestRegistryCache(): void;
/**
 * Get a plugin manifest record by ID.
 *
 * @param registry - The manifest registry
 * @param pluginId - The plugin ID to find
 * @returns The manifest record or undefined
 */
export declare function getPluginManifestById(registry: PluginManifestRegistry, pluginId: string): PluginManifestRecord | undefined;
/**
 * Get all plugins of a specific kind.
 *
 * @param registry - The manifest registry
 * @param kind - The plugin kind
 * @returns Array of matching manifest records
 */
export declare function getPluginsByKind(registry: PluginManifestRegistry, kind: PluginKind): PluginManifestRecord[];
/**
 * Get all plugins that provide a specific channel.
 *
 * @param registry - The manifest registry
 * @param channelId - The channel ID
 * @returns Array of matching manifest records
 */
export declare function getPluginsByChannel(registry: PluginManifestRegistry, channelId: string): PluginManifestRecord[];
/**
 * Get all plugins that provide a specific provider.
 *
 * @param registry - The manifest registry
 * @param providerId - The provider ID
 * @returns Array of matching manifest records
 */
export declare function getPluginsByProvider(registry: PluginManifestRegistry, providerId: string): PluginManifestRecord[];
//# sourceMappingURL=manifest-registry.d.ts.map