import { type IAgentRuntime, Service } from "../types";
export declare class VoiceCacheService extends Service {
    static serviceType: "voice_cache";
    capabilityDescription: string;
    private cacheDir;
    constructor();
    initialize(_runtime: IAgentRuntime): Promise<void>;
    stop(): Promise<void>;
    getCached(key: string): Buffer | null;
    setCached(key: string, audio: Buffer): void;
    generateKey(content: string, voiceId: string, model: string): string;
    shouldCache(content: string): boolean;
}
//# sourceMappingURL=voice-cache.d.ts.map